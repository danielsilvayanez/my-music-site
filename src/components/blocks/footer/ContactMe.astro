<footer class="footer" aria-label="Footer">
	<div class="footer__inner">
		<div class="footer__identity">
			<strong>Daniel Silva Yañez</strong>
			<span>Sänger &amp; Entertainer für Events</span>
		</div>
		<nav class="footer__links" aria-label="Kontakt">
			<a href="mailto:daniel.silva-yanez@gmx.de">E-Mail</a>
			<a href="https://www.instagram.com/daniel_silva_yanez/">Instagram</a>
		</nav>
		<small>© 2026 Daniel Silva Yañez</small>
	</div>

	<style>
		.footer {
			border-top: 1px solid #332b25;
			background: #171514;
			color: #d8d1c8;
		}
		.footer__inner {
			width: min(calc(100% - 3rem), 80rem);
			min-height: 9rem;
			margin-inline: auto;
			display: flex;
			align-items: center;
			justify-content: space-between;
			gap: 2rem;
		}
		.footer__identity {
			display: flex;
			flex-direction: column;
			gap: 0.35rem;
		}
		.footer__identity strong {
			color: #f2eee8;
			font-size: 1.05rem;
			font-weight: 600;
		}
		.footer__identity span,
		.footer small {
			color: #9d958b;
			font-size: 0.8rem;
		}
		.footer__links {
			display: flex;
			gap: 1.5rem;
		}
		.footer__links a {
			color: #f2eee8;
			font-size: 0.85rem;
			text-decoration: none;
			transition: color 200ms ease;
		}
		.footer__links a:hover,
		.footer__links a:focus-visible {
			color: #f28b3d;
		}
		.footer__links a:focus-visible {
			outline: 2px solid #f28b3d;
			outline-offset: 4px;
		}
		@media (max-width: 767px) {
			.footer__inner {
				width: min(calc(100% - 2.5rem), 80rem);
				min-height: 0;
				padding-block: 2rem;
				align-items: flex-start;
				flex-direction: column;
				gap: 1.25rem;
			}
		}
	</style>
</footer>
